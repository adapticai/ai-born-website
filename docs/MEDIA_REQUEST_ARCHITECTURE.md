# Media Request System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        AI-Born Landing Page                          │
│                     https://ai-born.org/#media                       │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      MediaRequestForm.tsx                            │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  • Name, Email, Outlet                                        │  │
│  │  • Request Type (dropdown)                                    │  │
│  │  • Phone (optional)                                           │  │
│  │  • Message (textarea)                                         │  │
│  │  • Deadline (datetime picker)                                 │  │
│  │  • Honeypot (hidden anti-spam)                               │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  Validation: React Hook Form + Zod Schema                           │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ POST /api/media-request
                                   │ Content-Type: application/json
                                   ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     API Route (route.ts)                             │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  1. Get Client IP                                             │  │
│  │  2. Check Rate Limit (5/hour per IP)                         │  │
│  │  3. Parse JSON Body                                           │  │
│  │  4. Validate with Zod Schema                                  │  │
│  │  5. Check Honeypot Field                                      │  │
│  │  6. Sanitize Inputs (XSS Prevention)                         │  │
│  │  7. Check Suspicious Content                                  │  │
│  │  8. Generate Request ID                                       │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
                    ▼                             ▼
    ┌───────────────────────────┐   ┌───────────────────────────┐
    │   Email Service           │   │   Storage Service         │
    │   (lib/email.ts)          │   │   (lib/utils/storage.ts)  │
    └───────────────────────────┘   └───────────────────────────┘
                    │                             │
                    ▼                             ▼
    ┌───────────────────────────┐   ┌───────────────────────────┐
    │  sendMediaRequest         │   │  storeSubmission()        │
    │  Notification()           │   │                           │
    │                           │   │  Writes to:               │
    │  • Format data            │   │  data/media-requests.json │
    │  • Generate HTML          │   │                           │
    │  • Send with retry        │   │  Format:                  │
    │    (5 attempts max)       │   │  {                        │
    │  • Exponential backoff    │   │    timestamp,             │
    │                           │   │    clientIp,              │
    │                           │   │    data: {...}            │
    │                           │   │  }                        │
    └───────────────────────────┘   └───────────────────────────┘
                    │
                    ▼
    ┌───────────────────────────┐
    │   Resend API              │
    │   (resend.com)            │
    │                           │
    │  • Sends HTML email       │
    │  • Returns message ID     │
    │  • Handles delivery       │
    └───────────────────────────┘
                    │
                    ▼
    ┌───────────────────────────┐
    │   PR Team Inbox           │
    │   press@micpress.com      │
    │                           │
    │  Email includes:          │
    │  • Request ID             │
    │  • Contact details        │
    │  • Request type           │
    │  • Message                │
    │  • Quick reply button     │
    └───────────────────────────┘
```

## Component Details

### 1. Frontend Components

```
src/components/
├── forms/
│   └── MediaRequestForm.tsx          ← Form component
│       ├── React Hook Form
│       ├── Zod validation
│       ├── Analytics tracking
│       └── Error handling
│
└── sections/
    └── BookMediaPress.tsx             ← Page section
        ├── Press kit assets grid
        ├── Quick action buttons
        └── MediaRequestForm instance
```

### 2. API Layer

```
src/app/api/
└── media-request/
    └── route.ts                       ← API endpoint
        ├── POST handler
        ├── OPTIONS handler (CORS)
        ├── GET handler (405 error)
        └── Rate limiting
```

### 3. Business Logic

```
src/lib/
├── email.ts                           ← Email service
│   ├── sendExcerptEmail()
│   ├── sendBonusPackEmail()
│   ├── sendMediaRequestNotification() ← New function
│   ├── sendMagicLinkEmail()
│   └── Email templates
│
├── api.ts                             ← API client
│   ├── submitMediaRequest()
│   ├── submitEmailCapture()
│   └── Error handling
│
├── schemas/
│   └── contact.ts                     ← Validation schemas
│       ├── mediaRequestSchema         ← Updated
│       ├── bulkOrderSchema
│       └── emailCaptureSchema
│
└── utils/
    ├── rate-limiter.ts                ← Rate limiting
    ├── sanitization.ts                ← XSS prevention
    └── storage.ts                     ← JSON file storage
```

### 4. Type Definitions

```
src/types/
├── forms.ts                           ← Form type definitions
│   ├── MediaRequestFormData
│   ├── EmailCaptureFormData
│   └── BulkOrderFormData
│
└── api.ts                             ← API response types
    ├── APIResponse<T>
    └── APIError
```

## Data Flow Diagram

### Happy Path

```
User fills form
    │
    ├─ Client validation (React Hook Form + Zod)
    │      └─ Invalid? → Show field errors
    │
    ▼
POST /api/media-request
    │
    ├─ Rate limit check
    │      └─ Exceeded? → 429 error + retry after
    │
    ├─ Server validation (Zod)
    │      └─ Invalid? → 400 error + field details
    │
    ├─ Honeypot check
    │      └─ Filled? → Silent rejection (200 OK to fool bots)
    │
    ├─ Sanitization (XSS prevention)
    │      └─ Suspicious? → 400 error
    │
    ├─ Generate request ID (MR-{timestamp}-{random})
    │
    ├─ Email notification (async)
    │      ├─ Format email HTML
    │      ├─ Send via Resend
    │      ├─ Retry on failure (5x, exponential backoff)
    │      └─ Log result
    │             └─ Success: Log message ID
    │             └─ Failure: Log error (but don't fail request)
    │
    ├─ Store in JSON file (async)
    │      └─ Log on failure (but don't fail request)
    │
    └─ Return 200 OK with request ID
           │
           └─ Client shows success message
                  └─ Reset form after 7 seconds
```

### Error Paths

```
┌─────────────────────────────────────────────────────────────┐
│                    Validation Errors (400)                   │
├─────────────────────────────────────────────────────────────┤
│ • Missing required fields (name, email, outlet, message)    │
│ • Invalid email format                                       │
│ • Message too short (<20 chars) or too long (>2000 chars)  │
│ • Invalid request type                                       │
│ • Invalid phone format (if provided)                        │
│ • Invalid deadline format (if provided)                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  Rate Limit Errors (429)                     │
├─────────────────────────────────────────────────────────────┤
│ • More than 5 requests in 1 hour from same IP               │
│ • Headers: X-RateLimit-*, Retry-After                      │
│ • Error message includes reset timestamp                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                 Suspicious Content (400)                     │
├─────────────────────────────────────────────────────────────┤
│ • XSS patterns detected in name, outlet, or message        │
│ • Script tags, event handlers, etc.                         │
│ • Logged with IP for security monitoring                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   Server Errors (500)                        │
├─────────────────────────────────────────────────────────────┤
│ • Unexpected exceptions                                      │
│ • Email service unavailable (request still succeeds)        │
│ • Storage service unavailable (request still succeeds)      │
│ • Generic error message (no internal details exposed)       │
└─────────────────────────────────────────────────────────────┘
```

## Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Security Layers                         │
└─────────────────────────────────────────────────────────────┘

Layer 1: Client-side
├─ React Hook Form validation (prevent bad UX)
└─ Honeypot field (catch simple bots)

Layer 2: Network
├─ CORS (only allowed origins)
└─ HTTPS (TLS encryption in production)

Layer 3: Rate Limiting
├─ Per-IP rate limits (5/hour)
├─ Per-email rate limits (5/hour)
└─ Distributed via Redis (production)

Layer 4: Input Validation
├─ Zod schema (type safety)
├─ Email format validation
├─ Length constraints
└─ Enum validation (request types)

Layer 5: Input Sanitization
├─ HTML entity encoding
├─ Script tag removal
├─ Event handler removal
└─ Suspicious pattern detection

Layer 6: Output Encoding
├─ Email HTML escaping
└─ JSON response encoding

Layer 7: Monitoring
├─ Request logging (IP, timestamp)
├─ Error tracking (Sentry)
└─ Suspicious activity alerts
```

## Email Template Structure

```
┌─────────────────────────────────────────────────────────────┐
│                     Email Template                           │
│                  (HTML with inline CSS)                      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        Header                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  AI-BORN                                              │  │
│  │  The Machine Core, the Human Cortex...               │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    Request Alert Box                         │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  🆕 New Media Request                                │  │
│  │  Request ID: MR-1234567890-ABC123                    │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                 Contact Information Table                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Contact Name:    │  Jane Doe                        │  │
│  │  Email:           │  jane@example.com (clickable)    │  │
│  │  Phone:           │  +1 (555) 123-4567 (clickable)  │  │
│  │  Outlet:          │  Tech Magazine                   │  │
│  │  Request Type:    │  Interview Request               │  │
│  │  Deadline:        │  Friday, 25 October 2025, 10:00 │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                       Message Box                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Message:                                             │  │
│  │  ────────────────────────────────────────────────────│  │
│  │  I would like to interview the author about          │  │
│  │  AI-native organisations for our December issue...   │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  Recommended Actions                         │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  • Respond within 24-48 hours                        │  │
│  │  • Verify outlet credentials for galleys             │  │
│  │  • Coordinate with author's calendar                 │  │
│  │  • Track in CRM/PR management system                 │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    Quick Reply Button                        │
│  ┌───────────────────────────────────────────────────────┐  │
│  │          [Reply to Jane Doe]  (button)               │  │
│  │  mailto:jane@example.com?subject=Re: AI-Born...      │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      Footer                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  AI-Born                                              │  │
│  │  Mic Press, LLC                                       │  │
│  │  New York, NY                                         │  │
│  │                                                        │  │
│  │  Unsubscribe | Privacy Policy                        │  │
│  │                                                        │  │
│  │  Submitted via ai-born.org at                        │  │
│  │  18 Oct 2025, 14:30 EST                              │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## Rate Limiting Architecture

### Development (In-Memory)

```
┌────────────────────────────────────┐
│  In-Memory Rate Limit Store       │
│  (Map<string, RateLimitData>)     │
├────────────────────────────────────┤
│  Key: "media-request:192.168.1.1" │
│  Value: {                          │
│    count: 3,                       │
│    resetAt: 1729270800000,        │
│    firstRequest: 1729267200000    │
│  }                                 │
└────────────────────────────────────┘

Pros:
• No external dependencies
• Fast (in-process)
• Free

Cons:
• Not shared across servers
• Resets on server restart
• Memory usage grows
```

### Production (Redis/Upstash)

```
┌────────────────────────────────────┐
│  Upstash Redis (Distributed)       │
│  https://upstash.com               │
├────────────────────────────────────┤
│  Key: "media-request:192.168.1.1" │
│  Value: 3                          │
│  TTL: 3600 seconds                 │
└────────────────────────────────────┘
         ▲
         │
    ┌────┴────┐
    │ Server 1 │
    └─────────┘
         ▲
         │
    ┌────┴────┐
    │ Server 2 │
    └─────────┘
         ▲
         │
    ┌────┴────┐
    │ Server 3 │
    └─────────┘

Pros:
• Shared across all servers
• Persists across restarts
• Auto-expiring keys (TTL)
• Scalable

Cons:
• Requires external service
• Network latency (~10ms)
• Cost (free tier: 10K commands/day)
```

## Monitoring & Observability

```
┌─────────────────────────────────────────────────────────────┐
│                      Logging Points                          │
└─────────────────────────────────────────────────────────────┘

1. Request Received
   [INFO] Media request from 192.168.1.1

2. Rate Limit Check
   [INFO] Rate limit OK: 2/5 requests used
   [WARN] Rate limit exceeded: 6/5 requests (resets at ...)

3. Validation
   [INFO] Validation passed
   [WARN] Validation failed: email: Invalid email format

4. Honeypot Detection
   [WARN] Honeypot triggered from 192.168.1.1

5. Suspicious Content
   [WARN] Suspicious content detected from 192.168.1.1

6. Email Sending
   [INFO] Sending PR notification for MR-xxx
   [INFO] Email sent successfully (Message ID: abc123)
   [ERROR] Email send failed: SMTP error

7. Storage
   [INFO] Stored media request MR-xxx
   [ERROR] Storage failed: Disk full

8. Response
   [INFO] Request processed in 450ms
   [INFO] Returned 200 OK with request ID MR-xxx

┌─────────────────────────────────────────────────────────────┐
│                         Metrics                              │
└─────────────────────────────────────────────────────────────┘

Request Volume:
• media_requests_total (counter)
• media_requests_by_type (counter, labeled by type)

Performance:
• media_request_duration_ms (histogram)
• email_delivery_duration_ms (histogram)

Errors:
• media_request_errors_total (counter, labeled by type)
• email_delivery_failures_total (counter)

Rate Limiting:
• rate_limit_hits_total (counter)
• rate_limit_remaining (gauge)

Business:
• media_requests_by_outlet (counter, labeled by outlet)
• urgent_requests_total (counter, where deadline < 24h)
```

## Scalability Considerations

### Current Capacity

```
Component          | Limit           | Notes
─────────────────────────────────────────────────────────────
API Endpoint       | ~1000 req/sec   | Next.js + Vercel
Rate Limiter       | 5 req/hour/IP   | Configurable
Email Service      | 50,000/month    | Resend free tier
JSON Storage       | ~10,000 records | Before performance degrades
Redis (Upstash)    | 10,000 cmd/day  | Free tier
```

### Scaling Path

```
Stage 1: MVP (Current)
├─ In-memory rate limiting
├─ JSON file storage
├─ Resend free tier
└─ Single server (Vercel)

Stage 2: Growth (<1000 requests/day)
├─ Upstash Redis rate limiting
├─ Migrate to database (Prisma + PostgreSQL)
├─ Resend paid tier
└─ Multiple Vercel instances

Stage 3: Scale (>1000 requests/day)
├─ Redis cluster
├─ Database read replicas
├─ Email queue (Bull/BullMQ)
├─ CDN for static assets
└─ Horizontal scaling

Stage 4: Enterprise (>10,000 requests/day)
├─ Dedicated email service
├─ CRM integration
├─ ML-based spam detection
├─ Real-time analytics dashboard
└─ Auto-scaling infrastructure
```

---

**Last Updated**: October 18, 2025
**Version**: 1.0.0
